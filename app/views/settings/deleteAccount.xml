<Alloy>
    <Window id="win" class="noActionBar">
        <ActionBar displayHomeAsUp="true" title="Configurações" onHomeIconItemSelected="close"/>
        <ScrollView>
            <View class="boxForm">
                <Require src="form/TextField" tipo="password" id="password" hintText="Senha Atual"></Require>
                <View class="form-group">
                    <Button class="btn" onClick="onDeleteClick">Deletar Conta</Button>
                </View>
            </View>
        </ScrollView>
    </Window>
</Alloy>
